pip install -r requirements.txt
fypenv\Scripts\activate
uvicorn backend.src.app.main:app --reload
uvicorn src.app.main:app --reload

docker-compose down           # Stops and removes old containers
docker-compose build          # Builds updated images
docker-compose up -d          # Starts containers in background

reminder--
root.render(
  // <React.StrictMode>
    <App />
  // </React.StrictMode>
);